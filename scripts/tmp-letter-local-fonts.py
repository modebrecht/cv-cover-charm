from pathlib import Path


def replace_once(path: str, old: str, new: str) -> None:
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise SystemExit(f"pattern not found in {path}: {old[:120]!r}")
    text = text.replace(old, new, 1)
    p.write_text(text)


# 1) Contextual control owns both font family + size.
p = Path("src/components/letter/LetterFontSizeControl.tsx")
p.write_text('''import { FONT_LABELS, type FontKey } from "@/components/cover/types";\nimport {\n  LETTER_BODY_FONT_SIZE_MAX,\n  LETTER_BODY_FONT_SIZE_MIN,\n} from "@/components/letter/types";\n\nexport function LetterFontSizeControl({\n  label,\n  value,\n  font,\n  fallbackSize,\n  min = LETTER_BODY_FONT_SIZE_MIN,\n  max = LETTER_BODY_FONT_SIZE_MAX,\n  hint,\n  onChange,\n  onFontChange,\n}: {\n  label: string;\n  value?: number;\n  font?: FontKey;\n  fallbackSize: number;\n  min?: number;\n  max?: number;\n  hint?: string;\n  onChange: (value: number | undefined) => void;\n  onFontChange: (value: FontKey | undefined) => void;\n}) {\n  const rawSize = value ?? fallbackSize;\n  const effectiveMin = Math.min(min, rawSize);\n  const effectiveMax = Math.max(max, rawSize);\n  const size = Math.min(effectiveMax, Math.max(effectiveMin, rawSize));\n\n  return (\n    <div\n      data-letter-context-font-size={label}\n      className="grid gap-2 rounded-md border bg-background/75 px-2.5 py-2"\n    >\n      <div className="flex items-start justify-between gap-3">\n        <div className="min-w-0">\n          <div className="text-xs font-medium">{label}</div>\n          <div className="text-[10px] leading-snug text-muted-foreground">\n            {hint ?? (font === undefined && value === undefined ? "Wie Vorlage" : "Eigene Typografie")}\n          </div>\n        </div>\n        <span className="shrink-0 text-xs font-medium tabular-nums">\n          {size.toFixed(size % 1 ? 1 : 0)} pt\n        </span>\n      </div>\n\n      <label className="grid gap-1 text-[10px] text-muted-foreground">\n        <span>Schriftart</span>\n        <select\n          data-letter-context-font-family={label}\n          value={font ?? "template"}\n          onChange={(event) =>\n            onFontChange(\n              event.target.value === "template" ? undefined : (event.target.value as FontKey),\n            )\n          }\n          className="w-full rounded-md border border-input bg-background px-2 py-1.5 text-xs text-foreground outline-none focus:ring-2 focus:ring-ring"\n          aria-label={`${label} Schriftart`}\n        >\n          <option value="template">Wie Vorlage</option>\n          {(Object.entries(FONT_LABELS) as Array<[FontKey, string]>).map(([key, fontLabel]) => (\n            <option key={key} value={key}>\n              {fontLabel}\n            </option>\n          ))}\n        </select>\n      </label>\n\n      <input\n        type="range"\n        min={effectiveMin}\n        max={effectiveMax}\n        step={0.5}\n        value={size}\n        onChange={(event) => onChange(Number(event.target.value))}\n        className="w-full accent-primary"\n        aria-label={`${label} Schriftgrösse`}\n      />\n      {font !== undefined || value !== undefined ? (\n        <button\n          type="button"\n          className="justify-self-start text-[10px] font-medium text-muted-foreground underline decoration-dotted underline-offset-2 hover:text-foreground"\n          onClick={() => {\n            onFontChange(undefined);\n            onChange(undefined);\n          }}\n        >\n          Wie Vorlage\n        </button>\n      ) : null}\n    </div>\n  );\n}\n''')

# 2) Persist local font family choices for roles that previously only had size fields.
path = "src/components/letter/types.ts"
replace_once(path, '  /** Schriftgrösse für Ort & Datum; fehlt = Vorlagengrösse. */\n  dateFontSizePt?: number;\n', '  /** Schriftart für Ort & Datum; fehlt = wie Vorlage. */\n  dateFont?: FontKey;\n  /** Schriftgrösse für Ort & Datum; fehlt = Vorlagengrösse. */\n  dateFontSizePt?: number;\n')
replace_once(path, '  /** Schriftgrösse für die Anrede; fehlt = Vorlagengrösse. */\n  salutationFontSizePt?: number;\n  /** Globale Schriftgrösse des eigentlichen Brief-Fliesstexts; fehlt = Vorlagengrösse. */\n  bodyFontSizePt?: number;\n  /** Schriftgrösse für die Grussformel; fehlt = Vorlagengrösse. */\n  closingFontSizePt?: number;\n  /** Schriftgrösse für den gedruckten Namen / die Unterschrift; fehlt = Vorlagengrösse. */\n  signatureFontSizePt?: number;\n  /** Schriftgrösse für Beilagen im Briefinhalt; fehlt = Vorlagengrösse. */\n  attachmentsFontSizePt?: number;\n', '  /** Schriftart für die Anrede; fehlt = wie Vorlage. */\n  salutationFont?: FontKey;\n  /** Schriftgrösse für die Anrede; fehlt = Vorlagengrösse. */\n  salutationFontSizePt?: number;\n  /** Schriftart des eigentlichen Brief-Fliesstexts; fehlt = wie Vorlage. */\n  bodyFont?: FontKey;\n  /** Globale Schriftgrösse des eigentlichen Brief-Fliesstexts; fehlt = Vorlagengrösse. */\n  bodyFontSizePt?: number;\n  /** Schriftart für die Grussformel; fehlt = wie Vorlage. */\n  closingFont?: FontKey;\n  /** Schriftgrösse für die Grussformel; fehlt = Vorlagengrösse. */\n  closingFontSizePt?: number;\n  /** Schriftart für den gedruckten Namen / die Unterschrift; fehlt = wie Vorlage. */\n  signatureFont?: FontKey;\n  /** Schriftgrösse für den gedruckten Namen / die Unterschrift; fehlt = Vorlagengrösse. */\n  signatureFontSizePt?: number;\n  /** Schriftart für Beilagen; fehlt = wie Vorlage. */\n  attachmentsFont?: FontKey;\n  /** Schriftgrösse für Beilagen im Briefinhalt; fehlt = Vorlagengrösse. */\n  attachmentsFontSizePt?: number;\n')
replace_once(path, 'export function withLetterRoleFontSize(\n  value: LetterRoleTypography | undefined,\n  fontSizePt: number | undefined,\n): LetterRoleTypography | undefined {\n', 'export function withLetterRoleFont(\n  value: LetterRoleTypography | undefined,\n  font: FontKey | undefined,\n): LetterRoleTypography | undefined {\n  const next: LetterRoleTypography = { ...(value ?? {}) };\n  if (font === undefined) delete next.font;\n  else next.font = font;\n  return Object.keys(next).length ? next : undefined;\n}\n\nexport function withLetterRoleFontSize(\n  value: LetterRoleTypography | undefined,\n  fontSizePt: number | undefined,\n): LetterRoleTypography | undefined {\n')
replace_once(path, '    recipientTypography: normalizeLetterRoleTypography(incoming.recipientTypography),\n    dateFontSizePt: normalizeLetterBodyFontSizePt(incoming.dateFontSizePt),\n    subjectTypography: normalizeLetterRoleTypography(incoming.subjectTypography),\n    salutationFontSizePt: normalizeLetterBodyFontSizePt(incoming.salutationFontSizePt),\n    bodyFontSizePt: normalizeLetterBodyFontSizePt(incoming.bodyFontSizePt),\n    closingFontSizePt: normalizeLetterBodyFontSizePt(incoming.closingFontSizePt),\n    signatureFontSizePt: normalizeLetterBodyFontSizePt(incoming.signatureFontSizePt),\n    attachmentsFontSizePt: normalizeLetterBodyFontSizePt(incoming.attachmentsFontSizePt),\n', '    recipientTypography: normalizeLetterRoleTypography(incoming.recipientTypography),\n    dateFont:\n      typeof incoming.dateFont === "string" && incoming.dateFont in FONT_LABELS\n        ? (incoming.dateFont as FontKey)\n        : undefined,\n    dateFontSizePt: normalizeLetterBodyFontSizePt(incoming.dateFontSizePt),\n    subjectTypography: normalizeLetterRoleTypography(incoming.subjectTypography),\n    salutationFont:\n      typeof incoming.salutationFont === "string" && incoming.salutationFont in FONT_LABELS\n        ? (incoming.salutationFont as FontKey)\n        : undefined,\n    salutationFontSizePt: normalizeLetterBodyFontSizePt(incoming.salutationFontSizePt),\n    bodyFont:\n      typeof incoming.bodyFont === "string" && incoming.bodyFont in FONT_LABELS\n        ? (incoming.bodyFont as FontKey)\n        : undefined,\n    bodyFontSizePt: normalizeLetterBodyFontSizePt(incoming.bodyFontSizePt),\n    closingFont:\n      typeof incoming.closingFont === "string" && incoming.closingFont in FONT_LABELS\n        ? (incoming.closingFont as FontKey)\n        : undefined,\n    closingFontSizePt: normalizeLetterBodyFontSizePt(incoming.closingFontSizePt),\n    signatureFont:\n      typeof incoming.signatureFont === "string" && incoming.signatureFont in FONT_LABELS\n        ? (incoming.signatureFont as FontKey)\n        : undefined,\n    signatureFontSizePt: normalizeLetterBodyFontSizePt(incoming.signatureFontSizePt),\n    attachmentsFont:\n      typeof incoming.attachmentsFont === "string" && incoming.attachmentsFont in FONT_LABELS\n        ? (incoming.attachmentsFont as FontKey)\n        : undefined,\n    attachmentsFontSizePt: normalizeLetterBodyFontSizePt(incoming.attachmentsFontSizePt),\n')

# 3) Remove duplicate font-family selectors from Layout; keep advanced styling there for now.
path = "src/components/letter/LetterLayoutControls.tsx"
replace_once(path, 'import { FONT_LABELS, type FontKey } from "@/components/cover/types";\n', '')
replace_once(path, 'const selectClass =\n  "w-full rounded-md border border-input bg-background px-2 py-1.5 text-xs outline-none focus:ring-2 focus:ring-ring";\n', '')
font_select = '''      <label className="grid gap-1 text-xs">\n        <span className="text-muted-foreground">Schriftart</span>\n        <select\n          className={selectClass}\n          value={current.font ?? "template"}\n          onChange={(event) =>\n            patch({\n              font: event.target.value === "template" ? undefined : (event.target.value as FontKey),\n            })\n          }\n          aria-label={`${label} Schriftart`}\n        >\n          <option value="template">Wie Vorlage</option>\n          {(Object.entries(FONT_LABELS) as Array<[FontKey, string]>).map(([key, fontLabel]) => (\n            <option key={key} value={key}>\n              {fontLabel}\n            </option>\n          ))}\n        </select>\n      </label>\n\n\n'''
replace_once(path, font_select, '')
replace_once(path, '          <div className="text-xs font-semibold">Briefspezifische Positionen &amp; Typografie</div>\n          <p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">\n            Diese Einstellungen gelten nur fürs Motivationsschreiben. Schriftgrössen stellst du\n            direkt bei den passenden Formularbereichen ein; Header und Footer bleiben im eigenen Bereich.\n          </p>\n', '          <div className="text-xs font-semibold">Briefspezifische Positionen &amp; Formatierung</div>\n          <p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">\n            Schriftart und Schriftgrösse stellst du direkt beim jeweiligen Inhalt ein. Hier bleiben\n            Position, Ausrichtung und zusätzliche Textformatierung.\n          </p>\n')

# 4) Contextual controls in the form: each role receives its own family and size; remove global Schrift accordion.
path = "src/routes/anschreiben.tsx"
replace_once(path, 'import { FONT_LABELS, TEMPLATES, type FontKey } from "@/components/cover/types";\n', 'import { TEMPLATES } from "@/components/cover/types";\n')
replace_once(path, '  letterAttachmentValues,\n  letterFontSelection,\n  normalizeLetterSpacingMm,\n  normalizeLetterDesign,\n  withLetterFontSelection,\n  withLetterRoleFontSize,\n', '  letterAttachmentValues,\n  normalizeLetterSpacingMm,\n  normalizeLetterDesign,\n  withLetterRoleFont,\n  withLetterRoleFontSize,\n')
replace_once(path, '    farben: false,\n    typo: false,\n', '    farben: false,\n')
replace_once(path, '                    <div className="text-xs font-semibold">Schriftgrössen</div>\n                    <div className="text-[11px] text-muted-foreground">\n                      Jede Textrolle bleibt standardmässig bei der Vorlagengrösse.\n                    </div>\n', '                    <div className="text-xs font-semibold">Typografie</div>\n                    <div className="text-[11px] text-muted-foreground">\n                      Schriftart und Grösse lassen sich für jede Textrolle separat einstellen.\n                    </div>\n')
# Add font props/callbacks to each role control.
repls = [
('''                    value={design.dateFontSizePt}\n                    fallbackSize={9.5}\n                    onChange={(dateFontSizePt) =>\n                      setDesign((current) => ({ ...current, dateFontSizePt }))\n                    }\n''', '''                    value={design.dateFontSizePt}\n                    font={design.dateFont}\n                    fallbackSize={9.5}\n                    onChange={(dateFontSizePt) =>\n                      setDesign((current) => ({ ...current, dateFontSizePt }))\n                    }\n                    onFontChange={(dateFont) =>\n                      setDesign((current) => ({ ...current, dateFont }))\n                    }\n'''),
('''                    value={design.subjectTypography?.fontSizePt}\n                    fallbackSize={12}\n                    onChange={(fontSizePt) =>\n                      setDesign((current) => ({\n                        ...current,\n                        subjectTypography: withLetterRoleFontSize(\n                          current.subjectTypography,\n                          fontSizePt,\n                        ),\n                      }))\n                    }\n''', '''                    value={design.subjectTypography?.fontSizePt}\n                    font={design.subjectTypography?.font}\n                    fallbackSize={12}\n                    onChange={(fontSizePt) =>\n                      setDesign((current) => ({\n                        ...current,\n                        subjectTypography: withLetterRoleFontSize(\n                          current.subjectTypography,\n                          fontSizePt,\n                        ),\n                      }))\n                    }\n                    onFontChange={(font) =>\n                      setDesign((current) => ({\n                        ...current,\n                        subjectTypography: withLetterRoleFont(current.subjectTypography, font),\n                      }))\n                    }\n'''),
('''                    value={design.salutationFontSizePt}\n                    fallbackSize={10.5}\n                    onChange={(salutationFontSizePt) =>\n                      setDesign((current) => ({ ...current, salutationFontSizePt }))\n                    }\n''', '''                    value={design.salutationFontSizePt}\n                    font={design.salutationFont}\n                    fallbackSize={10.5}\n                    onChange={(salutationFontSizePt) =>\n                      setDesign((current) => ({ ...current, salutationFontSizePt }))\n                    }\n                    onFontChange={(salutationFont) =>\n                      setDesign((current) => ({ ...current, salutationFont }))\n                    }\n'''),
('''                    value={design.bodyFontSizePt}\n                    fallbackSize={10.5}\n                    onChange={(bodyFontSizePt) =>\n                      setDesign((current) => ({ ...current, bodyFontSizePt }))\n                    }\n''', '''                    value={design.bodyFontSizePt}\n                    font={design.bodyFont}\n                    fallbackSize={10.5}\n                    onChange={(bodyFontSizePt) =>\n                      setDesign((current) => ({ ...current, bodyFontSizePt }))\n                    }\n                    onFontChange={(bodyFont) =>\n                      setDesign((current) => ({ ...current, bodyFont }))\n                    }\n'''),
('''                    value={design.closingFontSizePt}\n                    fallbackSize={10.5}\n                    onChange={(closingFontSizePt) =>\n                      setDesign((current) => ({ ...current, closingFontSizePt }))\n                    }\n''', '''                    value={design.closingFontSizePt}\n                    font={design.closingFont}\n                    fallbackSize={10.5}\n                    onChange={(closingFontSizePt) =>\n                      setDesign((current) => ({ ...current, closingFontSizePt }))\n                    }\n                    onFontChange={(closingFont) =>\n                      setDesign((current) => ({ ...current, closingFont }))\n                    }\n'''),
('''                    value={design.signatureFontSizePt}\n                    fallbackSize={10.5}\n                    onChange={(signatureFontSizePt) =>\n                      setDesign((current) => ({ ...current, signatureFontSizePt }))\n                    }\n''', '''                    value={design.signatureFontSizePt}\n                    font={design.signatureFont}\n                    fallbackSize={10.5}\n                    onChange={(signatureFontSizePt) =>\n                      setDesign((current) => ({ ...current, signatureFontSizePt }))\n                    }\n                    onFontChange={(signatureFont) =>\n                      setDesign((current) => ({ ...current, signatureFont }))\n                    }\n'''),
('''                  value={\n                    chromeOptions.headerMode === "contact"\n                      ? (chromeOptions.headerFontSizePt ?? undefined)\n                      : design.senderTypography?.fontSizePt\n                  }\n                  fallbackSize={chromeOptions.headerMode === "contact" ? 14 : 9.5}\n''', '''                  value={\n                    chromeOptions.headerMode === "contact"\n                      ? (chromeOptions.headerFontSizePt ?? undefined)\n                      : design.senderTypography?.fontSizePt\n                  }\n                  font={design.senderTypography?.font}\n                  fallbackSize={chromeOptions.headerMode === "contact" ? 14 : 9.5}\n'''),
('''                  onChange={(fontSizePt) => {\n                    if (chromeOptions.headerMode === "contact") {\n                      patchDossierChrome("letter", { headerFontSizePt: fontSizePt ?? null });\n                      return;\n                    }\n                    setDesign((current) => ({\n                      ...current,\n                      senderTypography: withLetterRoleFontSize(\n                        current.senderTypography,\n                        fontSizePt,\n                      ),\n                    }));\n                  }}\n''', '''                  onChange={(fontSizePt) => {\n                    if (chromeOptions.headerMode === "contact") {\n                      patchDossierChrome("letter", { headerFontSizePt: fontSizePt ?? null });\n                      return;\n                    }\n                    setDesign((current) => ({\n                      ...current,\n                      senderTypography: withLetterRoleFontSize(\n                        current.senderTypography,\n                        fontSizePt,\n                      ),\n                    }));\n                  }}\n                  onFontChange={(font) =>\n                    setDesign((current) => ({\n                      ...current,\n                      senderTypography: withLetterRoleFont(current.senderTypography, font),\n                    }))\n                  }\n'''),
('''                  value={design.recipientTypography?.fontSizePt}\n                  fallbackSize={10}\n                  onChange={(fontSizePt) =>\n''', '''                  value={design.recipientTypography?.fontSizePt}\n                  font={design.recipientTypography?.font}\n                  fallbackSize={10}\n                  onChange={(fontSizePt) =>\n'''),
('''                    }))\n                  }\n                />\n                <Field\n                  label="Lehrbetrieb"\n''', '''                    }))\n                  }\n                  onFontChange={(font) =>\n                    setDesign((current) => ({\n                      ...current,\n                      recipientTypography: withLetterRoleFont(current.recipientTypography, font),\n                    }))\n                  }\n                />\n                <Field\n                  label="Lehrbetrieb"\n'''),
('''                  value={\n                    chromeOptions.footerMode === "details"\n                      ? (chromeOptions.footerFontSizePt ?? undefined)\n                      : design.attachmentsFontSizePt\n                  }\n                  fallbackSize={chromeOptions.footerMode === "details" ? 8.5 : 10}\n''', '''                  value={\n                    chromeOptions.footerMode === "details"\n                      ? (chromeOptions.footerFontSizePt ?? undefined)\n                      : design.attachmentsFontSizePt\n                  }\n                  font={design.attachmentsFont}\n                  fallbackSize={chromeOptions.footerMode === "details" ? 8.5 : 10}\n'''),
('''                  onChange={(fontSizePt) => {\n                    if (chromeOptions.footerMode === "details") {\n                      patchDossierChrome("letter", { footerFontSizePt: fontSizePt ?? null });\n                      return;\n                    }\n                    setDesign((current) => ({\n                      ...current,\n                      attachmentsFontSizePt: fontSizePt,\n                    }));\n                  }}\n''', '''                  onChange={(fontSizePt) => {\n                    if (chromeOptions.footerMode === "details") {\n                      patchDossierChrome("letter", { footerFontSizePt: fontSizePt ?? null });\n                      return;\n                    }\n                    setDesign((current) => ({\n                      ...current,\n                      attachmentsFontSizePt: fontSizePt,\n                    }));\n                  }}\n                  onFontChange={(attachmentsFont) =>\n                    setDesign((current) => ({ ...current, attachmentsFont }))\n                  }\n'''),
]
for old, new in repls:
    replace_once(path, old, new)
# Remove entire standalone Schrift accordion.
old = '''            <Section title="Schrift" open={open.typo} onToggle={() => toggle("typo")}>\n              <label className="block text-xs font-medium">\n                Schriftart\n                <select\n                  value={letterFontSelection(design)}\n                  onChange={(event) => {\n                    const value = event.target.value;\n                    setDesign((current) =>\n                      withLetterFontSelection(\n                        current,\n                        value === "template" ? "template" : (value as FontKey),\n                      ),\n                    );\n                  }}\n                  className={inputClass}\n                >\n                  {design.template !== "brief" ? (\n                    <option value="template">Passend zur Vorlage</option>\n                  ) : null}\n                  {Object.entries(FONT_LABELS).map(([key, label]) => (\n                    <option key={key} value={key}>\n                      {label}\n                    </option>\n                  ))}\n                </select>\n              </label>\n            </Section>\n'''
replace_once(path, old, '')

# 5) Renderer applies all local font families, including integrated header/footer content.
path = "src/components/letter/LetterCanvas.tsx"
replace_once(path, '        footerLabel="Beilagen"\n        footerDetails={geometry.footer.showAttachments ? beilagen : []}\n', '        footerLabel="Beilagen"\n        footerDetails={geometry.footer.showAttachments ? beilagen : []}\n        headerFontOverride={senderTypography?.font}\n        footerFontOverride={design.attachmentsFont}\n')
replace_once(path, '      data-letter-user-body-size={bodySize !== undefined ? "true" : undefined}\n', '      data-letter-user-body-font={design.bodyFont ? "true" : undefined}\n      data-letter-user-body-size={bodySize !== undefined ? "true" : undefined}\n')
replace_once(path, '          "--letter-user-body-size": bodySize !== undefined ? `${bodySize}pt` : undefined,\n', '          "--letter-user-body-font": design.bodyFont ? FONT_STACKS[design.bodyFont] : undefined,\n          "--letter-user-body-size": bodySize !== undefined ? `${bodySize}pt` : undefined,\n')
replace_once(path, '            fontSize:\n              design.dateFontSizePt !== undefined ? `${design.dateFontSizePt}pt` : undefined,\n', '            fontFamily: design.dateFont ? FONT_STACKS[design.dateFont] : undefined,\n            fontSize:\n              design.dateFontSizePt !== undefined ? `${design.dateFontSizePt}pt` : undefined,\n')
replace_once(path, '              style={{\n                fontSize:\n                  design.salutationFontSizePt !== undefined\n                    ? `${design.salutationFontSizePt}pt`\n                    : undefined,\n              }}\n', '              style={{\n                fontFamily: design.salutationFont ? FONT_STACKS[design.salutationFont] : undefined,\n                fontSize:\n                  design.salutationFontSizePt !== undefined\n                    ? `${design.salutationFontSizePt}pt`\n                    : undefined,\n              }}\n')
replace_once(path, '                style={{\n                  fontSize:\n                    design.closingFontSizePt !== undefined\n                      ? `${design.closingFontSizePt}pt`\n                      : undefined,\n                }}\n', '                style={{\n                  fontFamily: design.closingFont ? FONT_STACKS[design.closingFont] : undefined,\n                  fontSize:\n                    design.closingFontSizePt !== undefined\n                      ? `${design.closingFontSizePt}pt`\n                      : undefined,\n                }}\n')
replace_once(path, '                style={{\n                  marginTop: `${signatureGapMm}mm`,\n                  fontSize:\n', '                style={{\n                  marginTop: `${signatureGapMm}mm`,\n                  fontFamily: design.signatureFont ? FONT_STACKS[design.signatureFont] : undefined,\n                  fontSize:\n')
replace_once(path, '                style={{\n                  fontSize:\n                    design.attachmentsFontSizePt !== undefined\n', '                style={{\n                  fontFamily: design.attachmentsFont ? FONT_STACKS[design.attachmentsFont] : undefined,\n                  fontSize:\n                    design.attachmentsFontSizePt !== undefined\n')

path = "src/components/letter/letter-user-typography.css"
p = Path(path)
text = p.read_text()
needle = '/* Global body size is deliberately narrower than the role controls and user-owned. */\n'
if needle not in text:
    raise SystemExit("body css marker missing")
text = text.replace(needle, '/* Contextual body typography is user-owned. */\n[data-letter-page][data-letter-user-body-font="true"] [data-letter-pdf-richtext="body"],\n[data-letter-page][data-letter-user-body-font="true"] [data-letter-pdf-richtext="body"] * {\n  font-family: var(--letter-user-body-font) !important;\n}\n\n/* Global body size is deliberately narrower than the role controls and user-owned. */\n', 1)
p.write_text(text)

# 6) Shared chrome accepts letter-only header/footer font-family overrides without coupling them together.
path = "src/components/dossier/DossierHeaderFooterChrome.tsx"
replace_once(path, 'import { FONT_STACKS } from "@/components/cover/types";\n', 'import { FONT_STACKS, type FontKey } from "@/components/cover/types";\n')
replace_once(path, '  footerLeft,\n  footerRight,\n  documentContent,\n}: {\n', '  footerLeft,\n  footerRight,\n  documentContent,\n  headerFontOverride,\n  footerFontOverride,\n}: {\n')
replace_once(path, '  footerRight?: string;\n  documentContent?: DossierChromeDocumentContent;\n}) {\n', '  footerRight?: string;\n  documentContent?: DossierChromeDocumentContent;\n  headerFontOverride?: FontKey;\n  footerFontOverride?: FontKey;\n}) {\n')
replace_once(path, '  const textFontFamily = visualOptions.textFont ? FONT_STACKS[visualOptions.textFont] : undefined;\n', '  const textFontFamily = visualOptions.textFont ? FONT_STACKS[visualOptions.textFont] : undefined;\n  const headerTextFontFamily = headerFontOverride ? FONT_STACKS[headerFontOverride] : textFontFamily;\n  const footerTextFontFamily = footerFontOverride ? FONT_STACKS[footerFontOverride] : textFontFamily;\n')
# Apply explicit family to text-bearing header/footer roots.
for old, new in [
('''              color: headerTextColor,\n              fontSize: `${headerFontSizePt ?? 7.6}pt`,\n''', '''              color: headerTextColor,\n              fontFamily: headerTextFontFamily,\n              fontSize: `${headerFontSizePt ?? 7.6}pt`,\n'''),
('''                color: headerTextColor,\n                fontSize: `${headerFontSizePt ?? (stackedHeader ? 8 : 8.5)}pt`,\n''', '''                color: headerTextColor,\n                fontFamily: headerTextFontFamily,\n                fontSize: `${headerFontSizePt ?? (stackedHeader ? 8 : 8.5)}pt`,\n'''),
('''            color: headerTextColor,\n            lineHeight: 1.05,\n''', '''            color: headerTextColor,\n            fontFamily: headerTextFontFamily,\n            lineHeight: 1.05,\n'''),
('''            color: footerTextColor,\n            fontSize: `${footerFontSizePt ?? 6.6}pt`,\n''', '''            color: footerTextColor,\n            fontFamily: footerTextFontFamily,\n            fontSize: `${footerFontSizePt ?? 6.6}pt`,\n'''),
('''            color: footerTextColor,\n            fontSize: `${footerFontSizePt ?? 8.5}pt`,\n''', '''            color: footerTextColor,\n            fontFamily: footerTextFontFamily,\n            fontSize: `${footerFontSizePt ?? 8.5}pt`,\n'''),
]:
    replace_once(path, old, new)

# 7) Regression tests cover no standalone accordion + independent local families.
path = "tests/unit/letter-context-font-sizes.test.ts"
p = Path(path)
text = p.read_text()
text = text.replace('describe("contextual letter font sizes", () => {', 'describe("contextual letter typography", () => {', 1)
text = text.replace('  test("keeps all new role sizes optional and normalizes explicit values", () => {', '  test("keeps all new role typography optional and normalizes explicit values", () => {', 1)
text = text.replace('    expect(defaults.dateFontSizePt).toBeUndefined();\n', '    expect(defaults.dateFont).toBeUndefined();\n    expect(defaults.dateFontSizePt).toBeUndefined();\n    expect(defaults.bodyFont).toBeUndefined();\n    expect(defaults.attachmentsFont).toBeUndefined();\n', 1)
text = text.replace('      dateFontSizePt: 7,\n', '      dateFont: "modern",\n      bodyFont: "serioes",\n      attachmentsFont: "freundlich",\n      dateFontSizePt: 7,\n', 1)
text = text.replace('    expect(normalized.dateFontSizePt).toBe(8);\n', '    expect(normalized.dateFont).toBe("modern");\n    expect(normalized.bodyFont).toBe("serioes");\n    expect(normalized.attachmentsFont).toBe("freundlich");\n    expect(normalized.dateFontSizePt).toBe(8);\n', 1)
text = text.replace('  test("places size controls in the matching form accordions", () => {', '  test("places font family and size controls in the matching form accordions", () => {', 1)
insert = '''    expect(route).not.toContain('<Section title="Schrift"');\n    expect(route).not.toContain('open={open.typo}');\n    expect(route).toContain('font={design.dateFont}');\n    expect(route).toContain('font={design.bodyFont}');\n    expect(route).toContain('font={design.senderTypography?.font}');\n    expect(route).toContain('font={design.recipientTypography?.font}');\n    expect(route).toContain('font={design.attachmentsFont}');\n'''
text = text.replace('    expect(route).toContain(\'data-letter-context-font-sizes="brief"\');\n', '    expect(route).toContain(\'data-letter-context-font-sizes="brief"\');\n' + insert, 1)
text = text.replace('  test("removes duplicate size sliders from Layout", () => {\n', '  test("removes duplicate font family and size controls from Layout", () => {\n')
text = text.replace('    expect(layout).not.toContain("BodyFontSizeControl");\n', '    expect(layout).not.toContain("BodyFontSizeControl");\n    expect(layout).not.toContain("FONT_LABELS");\n    expect(layout).not.toContain("Schriftart</span>");\n')
text = text.replace('    expect(canvas).toContain("design.attachmentsFontSizePt");\n', '    expect(canvas).toContain("design.attachmentsFontSizePt");\n    expect(canvas).toContain("design.dateFont");\n    expect(canvas).toContain("design.bodyFont");\n    expect(canvas).toContain("design.attachmentsFont");\n')
p.write_text(text)

# Add a focused source-level test for shared header/footer overrides.
Path("tests/unit/letter-context-font-families.test.ts").write_text('''import { describe, expect, test } from "bun:test";\nimport { readFileSync } from "node:fs";\n\nconst control = readFileSync(\n  new URL("../../src/components/letter/LetterFontSizeControl.tsx", import.meta.url),\n  "utf8",\n);\nconst chrome = readFileSync(\n  new URL("../../src/components/dossier/DossierHeaderFooterChrome.tsx", import.meta.url),\n  "utf8",\n);\nconst css = readFileSync(\n  new URL("../../src/components/letter/letter-user-typography.css", import.meta.url),\n  "utf8",\n);\n\ndescribe("contextual letter font families", () => {\n  test("offers the font selector beside every contextual size control", () => {\n    expect(control).toContain("data-letter-context-font-family");\n    expect(control).toContain("Wie Vorlage");\n    expect(control).toContain("FONT_LABELS");\n  });\n\n  test("keeps contact header and attachment footer font overrides independent", () => {\n    expect(chrome).toContain("headerFontOverride?: FontKey");\n    expect(chrome).toContain("footerFontOverride?: FontKey");\n    expect(chrome).toContain("fontFamily: headerTextFontFamily");\n    expect(chrome).toContain("fontFamily: footerTextFontFamily");\n  });\n\n  test("forces an explicit body font through rich text descendants", () => {\n    expect(css).toContain('data-letter-user-body-font="true"');\n    expect(css).toContain("font-family: var(--letter-user-body-font) !important;");\n  });\n});\n''')

print("patched contextual letter font families")
