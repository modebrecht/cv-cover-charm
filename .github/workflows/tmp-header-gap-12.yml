name: Header gap smoke

on:
  push:
    branches:
      - templateFIX
    paths:
      - .github/workflows/tmp-header-gap-12.yml

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "header gap workflow is valid"
