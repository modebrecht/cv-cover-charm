name: One-off PDF regression test patch v3

on:
  push:
    branches:
      - fix/pdf-richtext-collision-20260917-v6

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/pdf-richtext-collision-20260917-v6
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Remove premature wrap mutation
        shell: bash
        run: |
          sed -i '311d' tests/e2e/letter-pdf-richtext.spec.ts
          bunx prettier --write tests/e2e/letter-pdf-richtext.spec.ts
      - name: Remove one-off patcher and commit
        shell: bash
        run: |
          rm .github/workflows/patch-pdf-richtext-test-v3.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/e2e/letter-pdf-richtext.spec.ts .github/workflows/patch-pdf-richtext-test-v3.yml
          git commit -m "test(pdf): mutate combined letter after export mount"
          git push origin HEAD:fix/pdf-richtext-collision-20260917-v6
