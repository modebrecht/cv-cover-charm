name: PR Recovery Final Gate

on:
  pull_request:
    branches: [main]

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Native repository PR workflows provide the release gate."
